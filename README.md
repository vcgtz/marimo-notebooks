# Marimo Notebooks
This repo brings together several notebooks about different ideas that I like to try out.

## Notebooks
**`github-branch-cleaner.py`**

Extract all the branches in a local repository, then check if those branches exist on the remote repo (origin); if they don't, the local branches are deleted. Be careful if you want to try this because it can delete branches that were not pushed to the remote.
